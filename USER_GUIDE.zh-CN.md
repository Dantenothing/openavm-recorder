# OpenAVM V4 使用说明

[首页](README.zh-CN.md) · [English](USER_GUIDE.md) · [下载](https://github.com/Dantenothing/openavm-recorder/releases/tag/v4.0.0)

## 安装或升级

1. 通过兼容车机现有的 App Lab 安装流程，安装 `OpenAVM-Recorder-V4-arm64-v8a.apk`。
2. 需要手机传输或编辑时，再在安卓手机安装 `OpenAVM-Companion-V4.apk`。手机端不是必装的。
3. 按所用功能授予权限。两端要求 Android 8.0 或以上，但这不代表所有摄像头和 USB 实现均兼容。
4. 应用 ID 和签名相同的旧版可以覆盖升级。不要为了升级先卸载：卸载可能清除应用内部录像和设置。自行构建的 APK 通常签名不同，不能覆盖官方安装。

车机正式包仅包含 ARM64 库；手机包包含 ARM64、ARMv7、x86 和 x86_64。两端正式包均不包含已暂停的自研哨兵 AI 模型或 OpenCV 运行库。

## USB 录像

连接车机可识别、可写的 U 盘，在录像/存储设置中选择 USB 优先，开始前确认界面显示的存储目标。选择普通或延时模式和所需摄像头，再开始录像。

视频直接写入 USB 的 `Download/OpenAVM`。设置、少量元数据、恢复日志和诊断仍在车机内部。USB 不可用时，在车辆和摄像头状态允许继续录像的情况下，同一次录像最多回退一次内置存储；V4 在已有离车断电证据时阻止这个回退。重新插入 U 盘不会把已回退的本次录像静默搬回 USB，下次开始前请确认存储目标。

USB 配额默认 30 GiB，支持 15/30/45 GiB 和自定义。自动清理只处理确认属于 OpenAVM 的文件，不处理原车哨兵和其他 USB 文件。重要录像请及时保护或导出，拔出 U 盘前先结束正在进行的录像或传输。

## 离车与延时模式

在车主车辆上，普通录像通常会在离车后继续约五分钟，再随着车辆供电/显示状态变化停止，有助于覆盖离车至原车哨兵启动间的一部分空档。这不是固定计时、保证停车覆盖或控制原车哨兵衔接。

延时模式有独立的停止策略。2×、5×、10× 已完成针对性实测，不等于所有更高倍速均已验证。V4 不保证车辆休眠期间应用始终唤醒，也不提供回车自动开始录像。

## 查看和管理录像

记录页区分内置录像、OpenAVM USB 录像和原车哨兵。同一次现代 OpenAVM 录像的一分钟分段会在列表中归组，方便统一播放；并不是自动合并为一个新的大 MP4。缺少会话信息的旧文件可能仍单独显示。

打开录像可以播放、切换分段，或选择需要的分段传手机。记录页的 USB 导出操作可以把旧 OpenAVM 内置录像复制到 U 盘，删除原文件前先确认 USB 副本。

多选和批量删除只处理本软件管理的录像。原厂 `/SentryMode/` 保持只读，不参与删除或配额清理。保护/保存的 OpenAVM 事件不会被自动清理。

## 首次连接手机

1. 手机开启热点，车机连接该热点；也可以使用可信的共同局域网。
2. 手机打开 OpenAVM Companion，开启接收服务。
3. 车机打开“手机”页，输入手机版显示的 IP/连接地址及六位配对码。
4. 配对成功后，在车机记录页选择整条录像或其中几段发送。

首次配对会被记住，通常无需重复输入配对码。后续连接同一网络并启动两端服务，必要时点击重连；热点地址变化时，核对手机当前显示的地址。清除数据、重装或重置配对后可能需要重新配对。

传输时保持热点开启，建议让手机版留在前台。不同安卓手机的后台限制不同。本地传输不要求互联网连接。

## 手机查看、编辑与直接 USB

点开已收到的视频即在详情页播放，播放控件、分段和导出操作在同一页。正方形单视角视频按正确比例显示，竖屏或带旋转信息的视频适应显示区域。

通过导出可以截取指定时段，或从支持的合成视频中提取一个视角，再保存/分享。单视角提取是手机导出功能，并不是车机单独录制前视的模式。

也可以将 U 盘直接连接安卓手机，必要时使用转接头，通过系统文件选择器授予对应目录权限，导入/查看支持的视频并使用编辑工具。USB 和文件系统兼容性取决于手机，两种连接方式并非所有操作完全相同。

## 制作解锁/闭锁音效

两端均可在工具箱导入支持的本地音乐或视频，选择所需片段，导出车机使用的 **WAV**。MP3 可以作为素材，最终文件是 WAV。素材需要能被设备正常访问和解码。

- 手机连接车机时，可通过车机把音效保存到车上的 USB。
- 手机直接连接 U 盘时，可将成品保存到这个 U 盘。
- 只用车机时，可以先用 Brave 等浏览器下载支持的文件，再导入车机工具箱、提取和裁剪音频，保存到 USB。

支持的车机 USB 音效目录是 `/Lock Status Tones/` 和 `/解闭锁音效/`。工具箱可以查看、试听和删除这些目录中的 WAV 文件，但无法识别或切换原车当前选中的音效，需要在原车设置中选择成品。

**原车音效列表有时不会立刻刷新。** 实测中，有时需要离车再回车，或在文件操作结束后重新插拔 USB，才会显示新音效。暂时看不到，不一定是制作或传输失败。

## 语言与诊断

两端支持英语、简体中文、繁体中文、泰语、越南语、阿拉伯语和跟随系统。欢迎母语使用者反馈翻译问题。

车机高级工具默认隐藏：在“设置 → 关于与隐私”连续点击版本号七次，再打开“开发者工具 → 录像诊断”，直接复制 JSON。如果报告分段，请保存同一份报告的全部分段，无需导出 ZIP。

可在关于区域关闭开发者模式。旧版已经开启的偏好会在升级后保留。

公开反馈时提供应用版本、车型/固件、录像模式与存储选择、时间线和复现步骤。发送诊断前先检查内容，参阅 [SECURITY.md](SECURITY.md)，不要公开个人资料或整段私人录像。

## 已知限制

- 当前依据车主设备的针对性测试，不代表所有车辆/固件兼容，也不是穷尽的长时间验收。
- 旧开发版曾出现关闭或卸载应用仍无法恢复的相机取流停滞，需要重启车机。车主当时观察原车 360 仍可用，目前 V4 测试报告未复现，不能据此保证任何情况下都不会发生。若原厂功能异常，请停止使用；必要时按车辆说明中的方法重启，并记录当时情况。
- RC2 的离车后额外内置小录像修复已获车主复测，报告未再出现；V4 保留相同录像实现。
- 满盘、反复强行拔盘、所有手机后台状态和多日配额轮转未完成穷尽验证。
- 自研停车哨兵和回车自动录像暂停；原车哨兵查看/传输保留。详见[研究状态](research/sentry/README_V4_ARCHIVE.md)。
