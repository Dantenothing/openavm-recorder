<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- Preserve the installed OpenAVM launcher component and its home-screen shortcut. -->
        <activity-alias android:name="com.dante.zeekrbridge.MainActivity"
            android:targetActivity="com.dante.zeekrcheck.MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>
    </application>
</manifest>
